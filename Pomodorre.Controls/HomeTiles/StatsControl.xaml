<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Pomodorre.Controls.HomeTiles.StatsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="5">
        <StackPanel Spacing="10">
            <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold" />
            <Grid Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                <Grid MaxHeight="200">
                    <TextBlock x:Name="EmptyText" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    
                    <lvc:CartesianChart
                        x:Name="Chart"
                        Series="{x:Bind Series, Mode=OneWay}"
                        XAxes="{x:Bind XAxes, Mode=OneWay}"
                        YAxes="{x:Bind YAxes, Mode=OneWay}"
                        TooltipPosition="Top" MaxHeight="200">
                    </lvc:CartesianChart>
                </Grid>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>


