<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Pomodorre.WinUI.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pomodorre.WinUI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:set="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Page.Resources>
        <local:StringEqualsConverter x:Key="StringEqualsConverter"/>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Page.Resources>

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <ScrollViewer HorizontalAlignment="Stretch" MaxWidth="1200" Padding="50,0,50,0">
            <StackPanel HorizontalAlignment="Stretch" Orientation="Vertical" Margin="0,0,0,20">
                <Grid HorizontalAlignment="Stretch">
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Style="{StaticResource DisplayTextBlockStyle}" FontSize="30">Settings</TextBlock>
                        <TextBlock Opacity="0.7" Grid.Column="1" TextAlignment="Right">
            v1.0-beta1<LineBreak/>created by shef3r.
                        </TextBlock>
                    </Grid>
                </Grid>
                <TextBlock Style="{StaticResource DisplayTextBlockStyle}" FontSize="14" Margin="3,10,0,10">Appearance</TextBlock>
                <set:SettingsExpander Header="Theme" Description="Choose your preferred color scheme.">
                    <set:SettingsExpander.HeaderIcon>
                        <FontIcon Glyph="&#xE771;" />
                    </set:SettingsExpander.HeaderIcon>
                    <set:SettingsExpander.Items>
                        <StackPanel Spacing="8" Padding="20" Background="{ThemeResource LayerFillColorDefaultBrush}">
                            <RadioButton Content="System default" IsChecked="{x:Bind ViewModel.ThemeMode, Mode=OneWay, Converter={StaticResource StringEqualsConverter}, ConverterParameter=System}" Checked="ThemeRadio_Checked" Tag="System"/>
                            <RadioButton Content="Light" IsChecked="{x:Bind ViewModel.ThemeMode, Mode=OneWay, Converter={StaticResource StringEqualsConverter}, ConverterParameter=Light}" Checked="ThemeRadio_Checked" Tag="Light"/>
                            <RadioButton Content="Dark" IsChecked="{x:Bind ViewModel.ThemeMode, Mode=OneWay, Converter={StaticResource StringEqualsConverter}, ConverterParameter=Dark}" Checked="ThemeRadio_Checked" Tag="Dark"/>
                        </StackPanel>
                    </set:SettingsExpander.Items>
                </set:SettingsExpander>

                <TextBlock Style="{StaticResource DisplayTextBlockStyle}" FontSize="14" Margin="3,10,0,10">Behavior</TextBlock>
                <set:SettingsCard HeaderIcon="Stop" Header="Kill background process on exit" Description="Stop the background worker when closing the app with an active session.">
                    <ToggleSwitch IsOn="{x:Bind ViewModel.KillBackgroundProcessOnExit, Mode=TwoWay}"/>
                </set:SettingsCard>

                <TextBlock Style="{StaticResource DisplayTextBlockStyle}" FontSize="14" Margin="3,10,0,10">App Service</TextBlock>
                <set:SettingsCard IsEnabled="False" HeaderIcon="Share" Header="Announce timer status to compatible apps" Description="Enable App Service to broadcast timer updates to other applications.">
                    <ToggleSwitch IsOn="{x:Bind ViewModel.ExposeAppService, Mode=TwoWay}"/>
                </set:SettingsCard>

                <TextBlock Style="{StaticResource DisplayTextBlockStyle}" FontSize="14" Margin="3,10,0,10">Manage Home Items</TextBlock>
                <set:SettingsCard HeaderIcon="Message" Header="Show welcome message" Description="Display a welcome message on the home page.">
                    <ToggleSwitch IsOn="{x:Bind ViewModel.HomeItems.ShowWelcomeMessage, Mode=TwoWay}"/>
                </set:SettingsCard>
                <set:SettingsCard Header="Show session stats" Description="Display session statistics on the home page.">
                    <set:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE9D2;" />
                    </set:SettingsCard.HeaderIcon>
                    <ToggleSwitch IsOn="{x:Bind ViewModel.HomeItems.ShowSessionStats, Mode=TwoWay}"/>
                </set:SettingsCard>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
