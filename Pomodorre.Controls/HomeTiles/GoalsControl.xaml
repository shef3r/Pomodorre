<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Pomodorre.Controls.HomeTiles.GoalsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="5">
        <StackPanel Spacing="10">
            <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold" />
            <Grid Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                <StackPanel Spacing="12">
                    <TextBlock x:Name="SubtitleText" FontSize="14" FontWeight="SemiBold" />
                    <ProgressBar Value="50" Maximum="100" Height="8" CornerRadius="4" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                    <TextBlock x:Name="ProgressText" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Right" />
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>

