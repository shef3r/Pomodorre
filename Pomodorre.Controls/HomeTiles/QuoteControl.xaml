<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Pomodorre.Controls.HomeTiles.QuoteControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="5">
        <StackPanel Spacing="10">
            <TextBlock FontSize="16" FontWeight="SemiBold">Quote of the day</TextBlock>
            <Grid Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                <StackPanel Spacing="10">
                    <TextBlock FontSize="14" FontStyle="Italic" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                        "Focus on being productive instead of busy."
                    </TextBlock>
                    <TextBlock FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Right" Foreground="{ThemeResource TextFillColorTertiaryBrush}">
                        - Tim Ferriss
                    </TextBlock>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
