<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Pomodorre.WinUI.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pomodorre.WinUI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:set="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Page.Resources>
        <local:StringEqualsConverter x:Key="StringEqualsConverter"/>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Page.Resources>

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <ScrollViewer HorizontalAlignment="Stretch" MaxWidth="1200" Padding="50,0,50,0">
            <StackPanel HorizontalAlignment="Stretch" Orientation="Vertical" Margin="0,0,0,20">
                <Grid HorizontalAlignment="Stretch">
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Uid="SettingsTitle" Style="{StaticResource DisplayTextBlockStyle}" FontSize="30"></TextBlock>
                        <TextBlock Opacity="0.7" Grid.Column="1" TextAlignment="Right">
            v<Run x:Name="Version"/>-beta<LineBreak/>created by shef3r.
                        </TextBlock>
                    </Grid>
                </Grid>
                <TextBlock x:Uid="SettingsAppearance" Style="{StaticResource DisplayTextBlockStyle}" FontSize="14" Margin="3,10,0,10"></TextBlock>
                <set:SettingsExpander x:Uid="SettingsTheme">
                    <set:SettingsExpander.HeaderIcon>
                        <FontIcon Glyph="&#xE771;" />
                    </set:SettingsExpander.HeaderIcon>
                    <set:SettingsExpander.Items>
                        <StackPanel Spacing="8" Padding="20" Background="{ThemeResource LayerFillColorDefaultBrush}">
                            <RadioButton x:Uid="ThemeSystem" IsChecked="{x:Bind ViewModel.ThemeMode, Mode=OneWay, Converter={StaticResource StringEqualsConverter}, ConverterParameter=System}" Checked="ThemeRadio_Checked" Tag="System"/>
                            <RadioButton x:Uid="ThemeLight" IsChecked="{x:Bind ViewModel.ThemeMode, Mode=OneWay, Converter={StaticResource StringEqualsConverter}, ConverterParameter=Light}" Checked="ThemeRadio_Checked" Tag="Light"/>
                            <RadioButton x:Uid="ThemeDark" IsChecked="{x:Bind ViewModel.ThemeMode, Mode=OneWay, Converter={StaticResource StringEqualsConverter}, ConverterParameter=Dark}" Checked="ThemeRadio_Checked" Tag="Dark"/>
                        </StackPanel>
                    </set:SettingsExpander.Items>
                </set:SettingsExpander>

                <TextBlock x:Uid="SettingsBehavior" Style="{StaticResource DisplayTextBlockStyle}" FontSize="14" Margin="3,10,0,10"></TextBlock>
                <set:SettingsCard x:Uid="SettingsKillProcess" HeaderIcon="Stop">
                    <ToggleSwitch IsOn="{x:Bind ViewModel.KillBackgroundProcessOnExit, Mode=TwoWay}"/>
                </set:SettingsCard>

                <TextBlock x:Uid="SettingsAppService" Style="{StaticResource DisplayTextBlockStyle}" FontSize="14" Margin="3,10,0,10"></TextBlock>
                <set:SettingsCard x:Uid="SettingsAnnounce" HeaderIcon="Share">
                    <ToggleSwitch IsOn="{x:Bind ViewModel.ExposeAppService, Mode=TwoWay}"/>
                </set:SettingsCard>

                <TextBlock x:Uid="SettingsHomeItems" Style="{StaticResource DisplayTextBlockStyle}" FontSize="14" Margin="3,10,0,10"></TextBlock>
                <set:SettingsCard x:Uid="SettingsShowWelcome" HeaderIcon="Message">
                    <ToggleSwitch IsOn="{x:Bind ViewModel.HomeItems.ShowWelcomeMessage, Mode=TwoWay}"/>
                </set:SettingsCard>
                <set:SettingsCard x:Uid="SettingsShowStats">
                    <set:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE9D2;" />
                    </set:SettingsCard.HeaderIcon>
                    <ToggleSwitch IsOn="{x:Bind ViewModel.HomeItems.ShowSessionStats, Mode=TwoWay}"/>
                </set:SettingsCard>
                <set:SettingsCard x:Uid="SettingsShowHistory">
                    <set:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE81C;" />
                    </set:SettingsCard.HeaderIcon>
                    <ToggleSwitch IsOn="{x:Bind ViewModel.HomeItems.ShowHistoryStats, Mode=TwoWay}"/>
                </set:SettingsCard>
                <set:SettingsCard x:Uid="SettingsShowQuote">
                    <set:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE134;" />
                    </set:SettingsCard.HeaderIcon>
                    <ToggleSwitch IsOn="{x:Bind ViewModel.HomeItems.ShowQuote, Mode=TwoWay}"/>
                </set:SettingsCard>
                <set:SettingsCard x:Uid="SettingsShowTips">
                    <set:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xE82F;" />
                    </set:SettingsCard.HeaderIcon>
                    <ToggleSwitch IsOn="{x:Bind ViewModel.HomeItems.ShowTips, Mode=TwoWay}"/>
                </set:SettingsCard>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>


