<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Pomodorre.WinUI.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pomodorre.WinUI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Page.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Page.Resources>

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <ScrollViewer HorizontalAlignment="Stretch" MaxWidth="1200" Padding="50,0,50,0">
            <StackPanel HorizontalAlignment="Stretch" Orientation="Vertical" Margin="0,0,0,20">
                <TextBlock x:Name="WelcomeMessage" 
           Visibility="{x:Bind Settings.ShowWelcomeMessage, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
           FontWeight="SemiBold" FontFamily="Segoe UI Variable Display" FontSize="30">
    Welcome to Pomodorre!
                </TextBlock>

                <ItemsRepeater x:Name="StaggeredRepeater" 
                    ItemsSource="{x:Bind HomeItemsCollection, Mode=OneWay}">
                    <ItemsRepeater.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="10" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                <ContentPresenter Content="{Binding}"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsRepeater.ItemTemplate>
                    <ItemsRepeater.Layout>
                        <controls:StaggeredLayout ColumnSpacing="10"
                                              DesiredColumnWidth="300"
                                              ItemsStretch="Fill"
                                              RowSpacing="10" />
                    </ItemsRepeater.Layout>
                </ItemsRepeater>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
