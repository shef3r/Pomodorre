# Copilot Instructions

## Project Guidelines
- User prefers x:Bind OneWay/TwoWay bindings over code-behind events for settings management. Theme switching should be instant across the entire app including title bar controls. Settings should be organized into nested classes (e.g., Settings.HomeItems) to avoid cluttering the main Settings class. All UI changes from settings should react instantly to changes.