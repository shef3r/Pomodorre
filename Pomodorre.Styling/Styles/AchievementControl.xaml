<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Pomodorre.Controls">

	<Style TargetType="local:AchievementControl">
		<Setter Property="MinWidth" Value="350"/>
		<Setter Property="MinHeight" Value="150"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="local:AchievementControl">
					<Grid
						HorizontalAlignment="Stretch"
						VerticalAlignment="Stretch"
						CornerRadius="7"
						BorderThickness="1"
						Padding="0"
						BorderBrush="{StaticResource CardStrokeColorDefaultBrush}"
						Background="{StaticResource CardBackgroundFillColorDefaultBrush}">
						<Grid.RowDefinitions>
							<RowDefinition Height="*"/>
							<RowDefinition Height="Auto"/>
						</Grid.RowDefinitions>
						<ProgressBar Grid.Row="1" Value="{TemplateBinding Value}" Maximum="{TemplateBinding MaxValue}" Minimum="0" Margin="0,0,0,-1" Background="{ThemeResource SubtleFillColorSecondaryBrush}">
							<ProgressBar.Resources>
								<x:Double x:Key="ProgressBarMinHeight">7</x:Double>
								<x:Double x:Key="ProgressBarTrackHeight">7</x:Double>
								<CornerRadius x:Key="ProgressBarCornerRadius">0,3.5,0,3.5</CornerRadius>
								<CornerRadius x:Key="ProgressBarTrackCornerRadius">0</CornerRadius>
							</ProgressBar.Resources>
							<ProgressBar.Foreground>
								<LinearGradientBrush StartPoint="0,1" EndPoint="1,1">
									<GradientStop x:Name="GradientStop1" Color="{ThemeResource SystemAccentColorDark1}" Offset="0"/>
									<GradientStop x:Name="GradientStop2" Color="{ThemeResource SystemAccentColorLight1}" Offset="0.7"/>
								</LinearGradientBrush>
							</ProgressBar.Foreground>
						</ProgressBar>
						<Grid Grid.Row="0" Padding="20">
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto"/>
								<RowDefinition Height="*"/>
								<RowDefinition Height="Auto"/>
							</Grid.RowDefinitions>
							<TextBlock FontWeight="SemiBold" FontFamily="Segoe UI Variable Display" Grid.Row="0" FontSize="25" MaxLines="1" TextTrimming="CharacterEllipsis" Text="{TemplateBinding Title}"/>
							<TextBlock Grid.Row="1" FontFamily="Segoe UI Variable Text" VerticalAlignment="Top" Margin="0,5,0,5" TextTrimming="CharacterEllipsis" MaxLines="2" TextWrapping="Wrap" Text="{TemplateBinding Description}"/>
							<Grid Grid.Row="2">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="Auto"/>
									<ColumnDefinition Width="Auto"/>
									<ColumnDefinition Width="*"/>
									<ColumnDefinition Width="Auto"/>
									<ColumnDefinition Width="Auto"/>
								</Grid.ColumnDefinitions>
								<TextBlock Opacity="0.7" TextAlignment="Left" Grid.Column="0" Text="{Binding StarValue, RelativeSource={RelativeSource TemplatedParent}}"/>
								<TextBlock Opacity="0.7" TextAlignment="Left" Grid.Column="1" Margin="2,-0.1,0,0">stars</TextBlock>
								<TextBlock Opacity="0.7" TextAlignment="Left" Grid.Column="3" Text="{Binding PercentageValue, RelativeSource={RelativeSource TemplatedParent}}"/>
								<TextBlock Opacity="0.7" TextAlignment="Left" Grid.Column="4">% complete</TextBlock>
							</Grid>
						</Grid>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
